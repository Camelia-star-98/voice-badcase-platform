# 🔍 网站无法打开排查指南

## 🌐 问题：https://voice-badcase-platform.vercel.app/status-flow 打不开

---

## 🔍 可能的原因

### 1. **环境变量未配置** ⭐（最可能）

Vercel 部署需要配置环境变量，如果 Supabase 配置缺失，页面可能无法加载。

**检查方法：**
1. 登录 Vercel Dashboard：https://vercel.com/dashboard
2. 选择项目 `voice-badcase-platform`
3. 进入 **Settings** → **Environment Variables**
4. 检查是否有以下变量：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

**解决方法：**
如果没有配置，添加：
```
VITE_SUPABASE_URL=https://mcpyilgpotajpmgblorc.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_kZPAb1ympgLgFITuM6T11g_5wKDbFCc
```

然后重新部署。

---

### 2. **数据库连接失败**

如果 Supabase 连接失败，页面可能无法加载数据，导致白屏或错误。

**检查方法：**
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签页
3. 查看是否有 Supabase 连接错误

**解决方法：**
- 确认 Supabase 项目正常运行
- 确认环境变量配置正确
- 确认数据库表已创建

---

### 3. **构建失败**

如果构建失败，网站可能无法正常部署。

**检查方法：**
1. 在 Vercel Dashboard 中
2. 查看 **Deployments** 标签页
3. 点击最新的部署记录
4. 查看构建日志，看是否有错误

**解决方法：**
- 如果有构建错误，根据错误信息修复
- 重新推送代码触发部署

---

### 4. **路由配置问题**

虽然路由配置看起来正确，但可能需要检查。

**检查方法：**
- 访问首页：https://voice-badcase-platform.vercel.app/
- 看是否能正常打开

**如果首页能打开，但 `/status-flow` 打不开：**
- 可能是该页面有错误
- 检查浏览器控制台错误信息

---

### 5. **数据库表未创建**

如果数据库表不存在，页面可能无法加载。

**检查方法：**
1. 在 Supabase Dashboard 中
2. 查看 Table Editor
3. 确认 `badcases` 表是否存在

**解决方法：**
- 如果表不存在，执行 `database/create_badcases_table.sql`
- 如果表存在但没有 `priority` 字段，执行 `database/add_priority_column.sql`

---

## 🔧 快速排查步骤

### 步骤1：检查 Vercel 部署状态

1. **访问 Vercel Dashboard**
   ```
   https://vercel.com/dashboard
   ```

2. **查看项目部署状态**
   - 选择项目 `voice-badcase-platform`
   - 查看最新的部署记录
   - 确认状态是 "Ready" 还是 "Error"

### 步骤2：检查环境变量

1. **进入项目设置**
   - Settings → Environment Variables

2. **确认配置**
   - `VITE_SUPABASE_URL` 已配置
   - `VITE_SUPABASE_ANON_KEY` 已配置

3. **如果缺失，添加后重新部署**

### 步骤3：检查浏览器控制台

1. **打开网站**
   ```
   https://voice-badcase-platform.vercel.app/status-flow
   ```

2. **打开开发者工具**（F12）

3. **查看错误信息**
   - Console 标签页：查看 JavaScript 错误
   - Network 标签页：查看网络请求是否失败

### 步骤4：检查数据库

1. **确认 Supabase 项目正常运行**
   - 访问：https://supabase.com/dashboard
   - 选择项目：mcpyilgpotajpmgblorc

2. **确认表已创建**
   - Table Editor → 查看 `badcases` 表是否存在

---

## 🛠️ 解决方法

### 方法1：重新配置环境变量并部署

1. **在 Vercel 中添加环境变量**
   ```
   VITE_SUPABASE_URL=https://mcpyilgpotajpmgblorc.supabase.co
   VITE_SUPABASE_ANON_KEY=sb_publishable_kZPAb1ympgLgFITuM6T11g_5wKDbFCc
   ```

2. **重新部署**
   - 在 Vercel Dashboard 中
   - 点击 **Redeploy** 按钮

### 方法2：检查并修复代码

1. **检查是否有构建错误**
   ```bash
   cd /Users/yanglurui/voice-badcase-platform
   npm run build
   ```

2. **如果有错误，修复后重新部署**

### 方法3：检查数据库

1. **确认数据库表已创建**
2. **如果未创建，执行初始化脚本**

---

## 📋 检查清单

完成以下检查：

- [ ] ✅ Vercel 部署状态正常（不是 Error）
- [ ] ✅ 环境变量已配置（VITE_SUPABASE_URL 和 VITE_SUPABASE_ANON_KEY）
- [ ] ✅ Supabase 项目正常运行
- [ ] ✅ 数据库表已创建（badcases 表存在）
- [ ] ✅ 浏览器控制台没有严重错误
- [ ] ✅ 网络请求正常（Network 标签页）

---

## 🎯 最可能的原因

根据经验，**最可能的原因是环境变量未在 Vercel 中配置**。

**快速解决：**
1. 登录 Vercel Dashboard
2. 进入项目设置 → Environment Variables
3. 添加 Supabase 配置
4. 重新部署

---

## 📞 需要帮助？

如果以上方法都无法解决，请提供：
1. Vercel 部署日志（如果有错误）
2. 浏览器控制台错误信息
3. Supabase 项目状态

我可以帮你进一步排查！




