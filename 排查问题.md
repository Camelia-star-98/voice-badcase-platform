# 🔍 排查问题：列表没有显示数据

## ✅ 关于数据库迁移的安全性

**数据库迁移不会改动原有数据！**

迁移脚本只是：
- ✅ 添加一个新字段 `priority`（如果不存在）
- ✅ 给新字段设置默认值 `'medium'`
- ✅ 创建索引提升查询性能

**现有数据完全不受影响：**
- 所有现有记录会自动获得 `priority = 'medium'`
- 不会删除、修改任何现有数据
- 不会影响现有字段

---

## 🔍 问题排查：列表没有显示数据

### 可能的原因

1. **数据库中没有数据**
   - 表是空的，所以列表为空

2. **Supabase 连接问题**
   - 配置错误或连接失败

3. **数据加载失败**
   - API 调用出错

4. **浏览器控制台有错误**
   - JavaScript 错误阻止了数据加载

---

## 📋 排查步骤

### 第1步：检查数据库是否有数据

1. **访问 Supabase Dashboard**
   ```
   https://supabase.com/dashboard
   ```

2. **查看数据**
   - 选择你的项目
   - 点击左侧菜单 **Table Editor**
   - 选择 `badcases` 表
   - 查看是否有数据

**如果没有数据：**
- 这是正常的，表是空的
- 需要先创建一些测试数据

**如果有数据但不显示：**
- 继续下面的排查步骤

---

### 第2步：检查浏览器控制台

1. **打开浏览器开发者工具**
   - 按 `F12` 或右键 → **检查**
   - 切换到 **Console** 标签页

2. **查看错误信息**
   - 是否有红色错误？
   - 是否有 Supabase 连接错误？
   - 是否有 API 调用失败？

**常见错误：**
- `Failed to fetch` → 网络或配置问题
- `Invalid API key` → Key 配置错误
- `relation "badcases" does not exist` → 表不存在或名称不对

---

### 第3步：检查 Supabase 配置

确认 `.env.local` 文件配置正确：

```bash
cd /Users/yanglurui/voice-badcase-platform
cat .env.local
```

应该看到：
```
VITE_SUPABASE_URL=https://mcpyilgpotajpmgblorc.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_kZPAb1ympgLgFITuM6T11g_5wKDbFCc
```

---

### 第4步：检查服务器是否正常运行

1. **查看终端输出**
   - 应该看到类似：
     ```
     VITE v5.0.8  ready in xxx ms
     ➜  Local:   http://localhost:3005/
     ```

2. **确认端口**
   - 访问：http://localhost:3005
   - 应该能看到页面（即使没有数据）

---

### 第5步：测试创建数据

如果数据库是空的，先创建一条测试数据：

1. **在平台中创建**
   - 访问：http://localhost:3005/badcase-list
   - 点击 **"新建Badcase"** 按钮
   - 填写表单，选择优先级
   - 提交

2. **在 Supabase 中创建（可选）**
   - 在 Table Editor 中
   - 点击 **Insert row**
   - 填写必填字段：
     - `id`: BC000001（或任意唯一ID）
     - `date`: 2025-12-29
     - `category`: 读音错误
     - `status`: pending
     - `description`: 测试数据
     - `priority`: medium（或 high/low）

---

## 🐛 常见问题解决

### 问题1：数据库是空的

**解决方法：**
- 创建一些测试数据
- 使用"新建Badcase"功能创建

### 问题2：连接失败

**检查：**
- `.env.local` 配置是否正确
- Supabase 项目是否正常运行
- 网络连接是否正常

**解决方法：**
```bash
# 重新检查配置
cat .env.local

# 重启服务器
# 按 Ctrl+C 停止，然后重新运行
npm run dev
```

### 问题3：表不存在

**解决方法：**
- 在 Supabase SQL Editor 中执行：
  ```sql
  SELECT * FROM badcases LIMIT 1;
  ```
- 如果报错"表不存在"，需要先创建表（执行 `database/create_badcases_table.sql`）

### 问题4：权限问题

**检查：**
- 在 Supabase Dashboard 中
- Settings → API → 确认 RLS 策略允许读取

---

## ✅ 快速检查清单

- [ ] ✅ 数据库中有数据（在 Supabase Table Editor 中查看）
- [ ] ✅ `.env.local` 配置正确
- [ ] ✅ 服务器正常运行（http://localhost:3005 可以访问）
- [ ] ✅ 浏览器控制台没有错误
- [ ] ✅ 已执行数据库迁移（添加 priority 字段）

---

## 🎯 下一步

1. **先检查数据库是否有数据**
   - 如果没有，先创建一些测试数据

2. **检查浏览器控制台**
   - 查看是否有错误信息

3. **告诉我检查结果**
   - 我可以帮你进一步排查

---

**数据库迁移是安全的，不会影响现有数据！** ✅




