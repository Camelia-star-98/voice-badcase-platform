# 🚀 启动步骤（解决 ERR_CONNECTION_REFUSED）

## 问题原因
开发服务器没有运行，需要先完成以下步骤：

---

## 第1步：安装依赖（如果还没安装）

```bash
cd /Users/yanglurui/voice-badcase-platform
npm install
```

等待安装完成（可能需要几分钟）

---

## 第2步：配置环境变量

### 2.1 创建配置文件

```bash
cd /Users/yanglurui/voice-badcase-platform
cp .env.local.example .env.local
```

### 2.2 编辑配置文件

```bash
# 使用你喜欢的编辑器打开
nano .env.local
# 或
code .env.local
```

### 2.3 填入 Supabase 配置

在 `.env.local` 中填入：

```env
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon key
```

**如何获取配置：**
1. 访问：https://supabase.com/dashboard
2. 选择你的项目
3. Settings → API
4. 复制 Project URL 和 anon public key

---

## 第3步：启动开发服务器

```bash
cd /Users/yanglurui/voice-badcase-platform
npm run dev
```

**预期输出：**
```
VITE v5.0.8  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

---

## 第4步：访问平台

看到上面的输出后，在浏览器中打开：
```
http://localhost:5173
```

---

## ⚡ 一键执行（复制粘贴到终端）

```bash
cd /Users/yanglurui/voice-badcase-platform && \
npm install && \
cp .env.local.example .env.local && \
echo "✅ 请编辑 .env.local 文件，填入 Supabase 配置，然后运行: npm run dev"
```

---

## 🔍 检查清单

启动前确认：
- [ ] ✅ 已安装依赖（`npm install`）
- [ ] ✅ 已创建 `.env.local` 配置文件
- [ ] ✅ 已填入 Supabase URL 和 Key
- [ ] ✅ 已执行 `npm run dev` 启动服务器
- [ ] ✅ 终端显示 "ready" 和访问地址

---

## 🐛 如果还是无法访问

1. **检查终端输出**
   - 是否有错误信息？
   - 是否显示 "ready"？

2. **检查端口**
   - 如果显示其他端口（如 5174），使用那个端口访问

3. **检查防火墙**
   - 确保允许本地连接

4. **重启服务器**
   - 按 `Ctrl+C` 停止
   - 重新运行 `npm run dev`





