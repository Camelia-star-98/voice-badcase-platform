# 🚀 快速启动指南

本文档帮助你在 5 分钟内启动语音 Badcase 平台。

## ✅ 前提条件

- Node.js >= 16.0.0
- npm >= 7.0.0
- Supabase 账号（免费）

---

## 📋 步骤清单

### ☐ 第 1 步：安装依赖

```bash
cd voice-badcase-platform
npm install
```

### ☐ 第 2 步：创建 Supabase 项目

1. 访问 https://supabase.com 并登录
2. 点击 **"New Project"**
3. 填写项目信息：
   - Project Name: `voice-badcase-platform`
   - Database Password: 设置密码（请保存好）
   - Region: 选择 `Northeast Asia (Tokyo)` 或 `Southeast Asia (Singapore)`
4. 点击 **"Create new project"**
5. 等待 1-2 分钟完成创建

### ☐ 第 3 步：初始化数据库

1. 在 Supabase 项目页面，点击左侧 **"SQL Editor"**
2. 点击 **"New query"**
3. 复制并粘贴以下文件内容：
   ```
   database/create_badcases_table.sql
   ```
4. 点击 **"Run"** 执行
5. 看到 "Success" 提示即成功

### ☐ 第 4 步：获取 API 凭证

1. 点击左侧底部 **"Settings"**（齿轮图标）
2. 选择 **"API"**
3. 复制以下两项：
   - **Project URL**（如：`https://xxxxx.supabase.co`）
   - **anon public key**（以 `eyJ` 开头的长字符串）

### ☐ 第 5 步：配置环境变量

在项目根目录创建 `.env.local` 文件：

```env
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

替换为你刚才复制的值。

### ☐ 第 6 步：启动项目

```bash
npm run dev
```

### ☐ 第 7 步：验证

1. 浏览器自动打开 http://localhost:3000
2. 打开浏览器控制台（F12），应该看到：
   ```
   ✅ VITE_SUPABASE_URL: 已设置
   ✅ VITE_SUPABASE_ANON_KEY: 已设置
   ✅ Supabase 客户端已初始化
   ```

---

## 🎉 完成！

现在你可以：

1. 查看主页概览
2. 进入 "Badcase 列表" 页面
3. 点击 "上传 Excel" 导入数据
4. 查看 "状态流转" 可视化

---

## 🔧 可选配置

### 启用实时同步（推荐）

1. 在 Supabase Dashboard 中
2. 进入 **Database** → **Replication**
3. 找到 `badcases` 表并勾选
4. 点击保存

现在多个用户的操作会实时同步！

### 启用公共访问（可选）

如果需要匿名访问，执行：

```sql
-- 在 SQL Editor 中运行
-- 复制 database/enable_public_access.sql 内容
```

---

## ❓ 遇到问题？

### 环境变量未生效

**症状**：控制台显示 "❌ VITE_SUPABASE_URL: 未设置"

**解决**：
- 确认 `.env.local` 在项目根目录
- 确认变量名以 `VITE_` 开头
- 重启开发服务器（Ctrl+C 然后 `npm run dev`）

### 连接失败

**症状**：控制台显示 "❌ Supabase 连接失败"

**解决**：
- 检查 URL 和 Key 是否正确
- 确认 Supabase 项目正常运行
- 检查网络连接

### 表不存在

**症状**：错误提示 "relation 'badcases' does not exist"

**解决**：
- 确认已执行 `create_badcases_table.sql`
- 在 Supabase 的 Table Editor 中检查表是否存在

---

## 📚 详细文档

- [完整配置指南](./SUPABASE_SETUP.md)
- [项目 README](./README.md)

---

**祝使用愉快！** 🎊

