# ✅ 配置完成指南

## 📋 当前状态

- ✅ Node.js 已安装
- ✅ 依赖已安装（npm install 完成）
- ⏳ 需要配置环境变量
- ⏳ 需要执行数据库迁移
- ⏳ 需要启动服务器

---

## 🔧 第1步：配置环境变量

### 1.1 创建配置文件

配置文件模板已创建：`.env.local`

### 1.2 编辑配置文件

在终端中执行：

```bash
cd /Users/yanglurui/voice-badcase-platform
nano .env.local
# 或使用其他编辑器：code .env.local
```

### 1.3 填入 Supabase 配置

在 `.env.local` 文件中填入：

```env
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon key
```

**如何获取配置：**

1. **访问 Supabase Dashboard**
   ```
   https://supabase.com/dashboard
   ```

2. **选择你的项目**

3. **获取配置**
   - 点击左侧菜单 **Settings**（齿轮图标）
   - 选择 **API** 标签页
   - 复制以下信息：
     - **Project URL** → 填入 `VITE_SUPABASE_URL`
     - **anon public** key → 填入 `VITE_SUPABASE_ANON_KEY`

**示例：**
```env
VITE_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTczMzQwODU2MiwiZXhwIjoyMDQ4OTg0NTYyf0.xxxxxxxxxxxxx
```

### 1.4 保存文件

- 如果使用 `nano`：按 `Ctrl+X`，然后按 `Y`，再按回车
- 如果使用其他编辑器：直接保存

---

## 🗄️ 第2步：执行数据库迁移（添加优先级字段）

### 2.1 登录 Supabase Dashboard

1. 访问：https://supabase.com/dashboard
2. 选择你的项目

### 2.2 打开 SQL Editor

1. 点击左侧菜单 **"SQL Editor"**
2. 点击 **"New query"**

### 2.3 执行迁移脚本

1. **打开迁移脚本文件**
   - 在项目中找到：`database/add_priority_column.sql`
   - 或者直接复制下面的 SQL：

```sql
-- 添加 priority 字段（修复优先级）
ALTER TABLE public.badcases 
ADD COLUMN IF NOT EXISTS priority TEXT DEFAULT 'medium';

-- 创建索引以提升查询性能
CREATE INDEX IF NOT EXISTS idx_badcases_priority ON public.badcases(priority);
```

2. **执行脚本**
   - 复制 SQL 代码
   - 粘贴到 SQL Editor
   - 点击 **"Run"** 执行
   - ✅ 应该看到成功提示

### 2.4 验证字段添加成功

1. 点击左侧菜单 **"Table Editor"**
2. 选择 `badcases` 表
3. ✅ 确认能看到 `priority` 字段（类型为 TEXT，默认值为 'medium'）

---

## 🚀 第3步：启动开发服务器

### 3.1 启动服务器

在终端中执行：

```bash
cd /Users/yanglurui/voice-badcase-platform
npm run dev
```

### 3.2 查看启动信息

**预期输出：**
```
VITE v5.0.8  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

### 3.3 访问平台

在浏览器中打开：
```
http://localhost:5173
```

---

## ✅ 验证功能

启动成功后，验证"修复优先级"功能：

### 1. 新建 Badcase 时选择优先级
- 点击 **"新建Badcase"** 按钮
- 填写表单信息
- 找到 **"修复优先级"** 字段
- 选择：高、中、低（默认是"中"）
- 提交表单

### 2. 查看优先级显示
- 在列表页面，应该能看到 **"优先级"** 列
- 显示为带颜色的 Tag：
  - 高 = 红色
  - 中 = 橙色
  - 低 = 蓝色

### 3. 测试筛选功能
- 在筛选区域找到 **"全部优先级"** 下拉选择器
- 选择某个优先级，点击 **"搜索"**
- 确认列表只显示对应优先级的记录

---

## 📋 检查清单

完成以下所有步骤：

- [ ] ✅ 已编辑 `.env.local` 文件
- [ ] ✅ 已填入 Supabase URL 和 Key
- [ ] ✅ 已执行数据库迁移脚本（添加 priority 字段）
- [ ] ✅ 已执行 `npm run dev` 启动服务器
- [ ] ✅ 浏览器可以访问 http://localhost:5173
- [ ] ✅ 可以新建 Badcase 并选择优先级
- [ ] ✅ 列表页面显示优先级列
- [ ] ✅ 优先级筛选功能正常

---

## 🎉 完成！

完成以上步骤后，你就可以使用包含"修复优先级"功能的平台了！

---

## 🆘 遇到问题？

### 问题1：配置文件找不到

**解决方法：**
```bash
cd /Users/yanglurui/voice-badcase-platform
cp .env.local.example .env.local
```

### 问题2：启动失败

**检查：**
- 确认 `.env.local` 配置正确
- 确认 Supabase 项目正常运行
- 查看终端错误信息

### 问题3：数据库迁移失败

**检查：**
- 确认已登录正确的 Supabase 项目
- 确认 SQL 脚本格式正确
- 查看 SQL Editor 的错误信息

---

**祝你配置顺利！** 🚀




