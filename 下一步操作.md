# 🚀 下一步操作（安装好 Node.js 后）

## ✅ 当前状态
- ✅ Node.js 已安装（v24.12.0）
- ✅ npm 已安装（11.6.2）
- ❌ 项目依赖未安装
- ❌ 配置文件未创建

---

## 📋 操作步骤

### 第1步：安装项目依赖

在终端中执行：

```bash
cd /Users/yanglurui/voice-badcase-platform
npm install
```

**等待安装完成**（可能需要几分钟，会下载很多包）

---

### 第2步：创建配置文件

```bash
# 复制配置模板
cp .env.local.example .env.local

# 编辑配置文件
nano .env.local
# 或使用其他编辑器：code .env.local
```

**在 `.env.local` 中填入你的 Supabase 配置：**

```env
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon key
```

**如何获取配置：**
1. 访问：https://supabase.com/dashboard
2. 选择你的项目
3. 点击 **Settings** → **API**
4. 复制：
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public** key → `VITE_SUPABASE_ANON_KEY`

**保存文件：**
- 如果使用 `nano`：按 `Ctrl+X`，然后按 `Y`，再按回车
- 如果使用其他编辑器：直接保存

---

### 第3步：执行数据库迁移（添加优先级字段）

1. **登录 Supabase Dashboard**
   - 访问：https://supabase.com/dashboard
   - 选择你的项目

2. **打开 SQL Editor**
   - 点击左侧菜单 **"SQL Editor"**
   - 点击 **"New query"**

3. **执行迁移脚本**
   - 打开文件：`database/add_priority_column.sql`
   - 复制**全部内容**
   - 粘贴到 SQL Editor
   - 点击 **"Run"** 执行
   - ✅ 应该看到 "✅ Priority 字段添加成功！" 的提示

---

### 第4步：启动开发服务器

```bash
cd /Users/yanglurui/voice-badcase-platform
npm run dev
```

**看到以下输出表示启动成功：**
```
VITE v5.0.8  ready in xxx ms

➜  Local:   http://localhost:5173/
```

---

### 第5步：访问平台

在浏览器中打开：
```
http://localhost:5173
```

---

## ⚡ 一键执行（复制粘贴到终端）

```bash
# 进入项目目录
cd /Users/yanglurui/voice-badcase-platform

# 安装依赖
npm install

# 创建配置文件
cp .env.local.example .env.local

# 提示：需要编辑 .env.local 文件，填入 Supabase 配置
echo "✅ 请编辑 .env.local 文件，填入 Supabase 配置"
echo "然后执行数据库迁移，最后运行: npm run dev"
```

---

## 📋 检查清单

完成以下所有步骤：

- [ ] ✅ 已执行 `npm install`（安装依赖）
- [ ] ✅ 已创建 `.env.local` 文件
- [ ] ✅ 已填入 Supabase URL 和 Key
- [ ] ✅ 已执行数据库迁移脚本（添加 priority 字段）
- [ ] ✅ 已执行 `npm run dev` 启动服务器
- [ ] ✅ 浏览器可以访问 http://localhost:5173

---

## 🎉 完成！

完成以上步骤后，你就可以看到包含"修复优先级"功能的平台了！





