# 🎯 完成配置 - 详细步骤

## 📋 当前进度

- ✅ Node.js 已安装
- ✅ 依赖已安装
- ✅ 配置文件模板已创建
- ⏳ **需要完成以下3步**

---

## 🔧 第1步：配置 Supabase 连接（2分钟）

### 方法1：使用 VS Code（推荐）

```bash
cd /Users/yanglurui/voice-badcase-platform
code .env.local
```

### 方法2：使用 nano 编辑器

```bash
cd /Users/yanglurui/voice-badcase-platform
nano .env.local
```

### 填入配置

在文件中填入以下内容（替换为你的实际值）：

```env
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon key
```

### 如何获取配置值？

1. **打开浏览器，访问：**
   ```
   https://supabase.com/dashboard
   ```

2. **登录并选择你的项目**

3. **获取配置：**
   - 点击左侧菜单 **Settings**（齿轮图标 ⚙️）
   - 点击 **API** 标签页
   - 找到以下两个值：
     - **Project URL** → 复制，填入 `VITE_SUPABASE_URL`
     - **anon public** key → 复制，填入 `VITE_SUPABASE_ANON_KEY`

4. **保存文件**
   - VS Code：直接保存（Cmd+S）
   - nano：按 `Ctrl+X`，然后按 `Y`，再按回车

---

## 🗄️ 第2步：执行数据库迁移（3分钟）

### 2.1 打开 Supabase SQL Editor

1. **在 Supabase Dashboard 中**
   - 确保已选择你的项目
   - 点击左侧菜单 **SQL Editor**
   - 点击 **New query** 按钮

### 2.2 执行迁移脚本

**方法1：复制文件内容**

1. 在项目中打开文件：`database/add_priority_column.sql`
2. 复制**全部内容**
3. 粘贴到 Supabase SQL Editor
4. 点击 **Run** 按钮执行

**方法2：直接复制下面的 SQL**

复制以下代码，粘贴到 SQL Editor，然后点击 Run：

```sql
ALTER TABLE public.badcases 
ADD COLUMN IF NOT EXISTS priority TEXT DEFAULT 'medium';

CREATE INDEX IF NOT EXISTS idx_badcases_priority ON public.badcases(priority);
```

### 2.3 验证成功

执行后应该看到：
- ✅ 成功提示信息
- ✅ 或者没有错误（说明字段已存在）

---

## 🚀 第3步：启动开发服务器（1分钟）

### 在终端中执行：

```bash
cd /Users/yanglurui/voice-badcase-platform
npm run dev
```

### 看到以下输出表示成功：

```
VITE v5.0.8  ready in xxx ms

➜  Local:   http://localhost:5173/
```

### 访问平台

在浏览器中打开：
```
http://localhost:5173
```

---

## ✅ 验证功能

打开平台后，验证"修复优先级"功能：

1. **新建 Badcase**
   - 点击右上角 **"新建Badcase"** 按钮
   - 填写表单
   - 找到 **"修复优先级"** 字段
   - 选择：高、中、低
   - 提交

2. **查看优先级显示**
   - 列表页面应该显示 **"优先级"** 列
   - 显示为带颜色的 Tag（高=红色，中=橙色，低=蓝色）

3. **测试筛选**
   - 使用 **"全部优先级"** 筛选器
   - 选择某个优先级，点击搜索
   - 确认筛选功能正常

---

## 🎉 完成！

完成以上3步后，你就可以使用包含"修复优先级"功能的平台了！

---

## 🆘 如果遇到问题

### 问题1：配置文件找不到

```bash
cd /Users/yanglurui/voice-badcase-platform
ls -la .env.local
```

如果不存在，手动创建：
```bash
cat > .env.local << EOF
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon key
EOF
```

### 问题2：启动失败

检查：
- `.env.local` 配置是否正确
- Supabase 项目是否正常运行
- 查看终端错误信息

### 问题3：数据库迁移失败

检查：
- 是否选择了正确的 Supabase 项目
- SQL 脚本格式是否正确
- 查看 SQL Editor 的错误信息

---

**祝你配置顺利！** 🚀




