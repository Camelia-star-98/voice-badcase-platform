-- ============================================
-- 🔔 启用 Supabase Realtime 功能
-- ============================================
-- 
-- 作用：启用 badcases 表的实时数据同步功能
-- 使用场景：多用户同时使用系统时，可以实时看到其他人的数据变化
-- 
-- ⚠️ 重要：必须在 Supabase 控制台的 SQL Editor 中执行此脚本
-- 
-- 执行步骤：
-- 1. 登录 Supabase Dashboard: https://supabase.com/dashboard
-- 2. 选择项目：mcpyilgpotajpmgblorc
-- 3. 左侧菜单点击 "SQL Editor"
-- 4. 点击 "+ New query" 创建新查询
-- 5. 复制粘贴下面的 SQL 代码
-- 6. 点击 "Run" 按钮执行
-- ============================================

-- 1️⃣ 为 badcases 表启用 Realtime 复制
-- 将 badcases 表添加到 supabase_realtime 发布中
ALTER PUBLICATION supabase_realtime ADD TABLE badcases;

-- 2️⃣ 验证配置（可选，用于检查是否配置成功）
-- 查看已启用 Realtime 的表
SELECT 
    schemaname,
    tablename
FROM 
    pg_publication_tables
WHERE 
    pubname = 'supabase_realtime';

-- ✅ 如果看到 badcases 表在结果中，说明配置成功！
-- 
-- 预期输出：
-- ┌─────────────┬───────────┐
-- │ schemaname  │ tablename │
-- ├─────────────┼───────────┤
-- │ public      │ badcases  │
-- └─────────────┴───────────┘

-- ============================================
-- 📋 配置说明
-- ============================================
-- 
-- Realtime 功能启用后：
-- ✅ 多个用户可以实时看到数据变化
-- ✅ 新增、更新、删除操作会自动同步到所有在线用户
-- ✅ 无需手动刷新页面
-- 
-- 如果不需要 Realtime：
-- 可以执行以下命令禁用：
-- ALTER PUBLICATION supabase_realtime DROP TABLE badcases;
-- ============================================

