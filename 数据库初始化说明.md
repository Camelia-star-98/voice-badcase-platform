# 🗄️ 数据库初始化说明

## 📋 当前情况

你提供的 Supabase 项目：
- **URL**: https://mcpyilgpotajpmgblorc.supabase.co
- **状态**: 数据库是空的（这是正常的）

---

## 🔍 为什么数据库是空的？

### 可能的原因：

1. **新创建的项目**
   - 如果是刚创建的 Supabase 项目，数据库自然是空的
   - 需要先创建表和初始化数据

2. **表还没有创建**
   - `badcases` 表可能还没有创建
   - 需要执行建表脚本

3. **使用了不同的项目**
   - 可能配置的是一个新的测试项目
   - 而不是之前有数据的项目

---

## ✅ 解决方案：初始化数据库

### 第1步：检查表是否存在

在 Supabase Dashboard 中：

1. **访问**: https://supabase.com/dashboard
2. **选择项目**: mcpyilgpotajpmgblorc
3. **查看 Table Editor**
   - 点击左侧菜单 **Table Editor**
   - 查看是否有 `badcases` 表

**如果没有表：**
- 需要先创建表（执行第2步）

**如果有表但是空的：**
- 这是正常的，可以添加数据（执行第3步）

---

### 第2步：创建 badcases 表（如果不存在）

1. **打开 SQL Editor**
   - 在 Supabase Dashboard 中
   - 点击左侧菜单 **SQL Editor**
   - 点击 **New query**

2. **执行建表脚本**
   - 打开项目文件：`database/create_badcases_table.sql`
   - 复制**全部内容**
   - 粘贴到 SQL Editor
   - 点击 **Run** 执行

**或者直接复制下面的 SQL：**

```sql
-- 创建 badcases 表
CREATE TABLE IF NOT EXISTS public.badcases (
    id TEXT PRIMARY KEY,
    date TEXT NOT NULL,
    description TEXT NOT NULL,
    subject TEXT,
    category TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'pending',
    priority TEXT DEFAULT 'medium',
    location TEXT,
    full_tts_lesson_id TEXT,
    cms_id TEXT,
    reporter TEXT,
    expected_fix_date TEXT,
    audio_url TEXT,
    video_url TEXT,
    model_id TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_badcases_date ON public.badcases(date);
CREATE INDEX IF NOT EXISTS idx_badcases_status ON public.badcases(status);
CREATE INDEX IF NOT EXISTS idx_badcases_category ON public.badcases(category);
CREATE INDEX IF NOT EXISTS idx_badcases_subject ON public.badcases(subject);
CREATE INDEX IF NOT EXISTS idx_badcases_priority ON public.badcases(priority);
CREATE INDEX IF NOT EXISTS idx_badcases_created_at ON public.badcases(created_at DESC);

-- 启用 RLS
ALTER TABLE public.badcases ENABLE ROW LEVEL SECURITY;

-- 创建公开访问策略
CREATE POLICY "允许所有人查看 badcases"
ON public.badcases FOR SELECT TO anon, authenticated USING (true);

CREATE POLICY "允许所有人插入 badcases"
ON public.badcases FOR INSERT TO anon, authenticated WITH CHECK (true);

CREATE POLICY "允许所有人更新 badcases"
ON public.badcases FOR UPDATE TO anon, authenticated USING (true) WITH CHECK (true);

CREATE POLICY "允许所有人删除 badcases"
ON public.badcases FOR DELETE TO anon, authenticated USING (true);

-- 启用 Realtime
ALTER PUBLICATION supabase_realtime ADD TABLE public.badcases;
```

3. **验证表创建成功**
   - 执行后应该看到成功提示
   - 在 Table Editor 中应该能看到 `badcases` 表

---

### 第3步：添加优先级字段（如果表已存在但没有 priority 字段）

如果表已经存在，但还没有 `priority` 字段：

1. **执行迁移脚本**
   - 打开文件：`database/add_priority_column.sql`
   - 复制全部内容
   - 在 SQL Editor 中执行

**或者直接执行：**

```sql
ALTER TABLE public.badcases 
ADD COLUMN IF NOT EXISTS priority TEXT DEFAULT 'medium';

CREATE INDEX IF NOT EXISTS idx_badcases_priority ON public.badcases(priority);
```

---

### 第4步：添加测试数据（可选）

#### 方法1：在平台中创建

1. **访问平台**
   ```
   http://localhost:3005/badcase-list
   ```

2. **创建测试数据**
   - 点击 **"新建Badcase"** 按钮
   - 填写表单：
     - 学科：英语
     - 出现位置：行课互动部分
     - CMS课节ID：12345
     - 问题提报人：测试用户
     - 问题模型ID：选择任意一个
     - 分类：读音错误
     - 修复优先级：高（测试优先级功能）
     - 期望修复时间：选择日期
     - 问题描述：这是测试数据，用于验证优先级功能
   - 点击 **"提交"**

#### 方法2：在 Supabase 中直接插入

在 SQL Editor 中执行：

```sql
INSERT INTO public.badcases (
  id, date, subject, location, cms_id, reporter, 
  category, status, priority, description
) VALUES (
  'BC000001',
  '2025-12-29',
  'english',
  'interactive',
  '12345',
  '测试用户',
  '读音错误',
  'pending',
  'high',
  '这是测试数据，用于验证优先级功能'
);
```

---

## 📋 完整初始化流程

如果你是从零开始，按以下顺序执行：

1. ✅ **创建表**（执行 `database/create_badcases_table.sql`）
2. ✅ **添加优先级字段**（执行 `database/add_priority_column.sql`）
3. ✅ **添加测试数据**（在平台中创建或直接插入）

---

## 🔍 检查清单

完成初始化后，确认：

- [ ] ✅ `badcases` 表已创建
- [ ] ✅ `priority` 字段已添加
- [ ] ✅ 表中有数据（至少一条测试数据）
- [ ] ✅ 平台可以正常显示数据
- [ ] ✅ 优先级功能正常工作

---

## 🎯 总结

**数据库为空是正常的**，特别是：
- 新创建的 Supabase 项目
- 新配置的项目
- 测试环境

**解决方法：**
1. 先创建表（如果不存在）
2. 添加优先级字段
3. 创建一些测试数据

---

**需要我帮你检查表是否存在，或者创建测试数据吗？** 🚀




