# 🚀 快速启动指南

## 一、安装依赖

打开终端，进入项目目录：

```bash
cd /Users/ailian/Downloads/voice-badcase-platform
```

安装项目依赖（选择其中一种方式）：

### 方式1：使用 npm
```bash
npm install
```

### 方式2：使用 yarn
```bash
yarn install
```

### 方式3：使用 pnpm（推荐，更快）
```bash
pnpm install
```

**注意**：首次安装可能需要几分钟时间，请耐心等待。

---

## 二、启动开发服务器

安装完成后，运行以下命令启动开发服务器：

### 使用 npm
```bash
npm run dev
```

### 使用 yarn
```bash
yarn dev
```

### 使用 pnpm
```bash
pnpm dev
```

---

## 三、访问应用

启动成功后，您会看到类似以下的输出：

```
VITE v5.0.8  ready in 500 ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

浏览器会自动打开，如果没有自动打开，请手动访问：

**http://localhost:3000**

---

## 四、页面导航

应用启动后，您可以访问以下页面：

- 🏠 **首页**: http://localhost:3000/home
  - 查看平台概览和功能介绍
  
- 📊 **数据可视化**: http://localhost:3000/visualization
  - 查看各种统计图表和数据分析
  
- 📝 **Badcase列表**: http://localhost:3000/badcase-list
  - 浏览和管理所有Badcase数据

---

## 五、常用命令

### 开发模式
```bash
npm run dev       # 启动开发服务器（支持热更新）
```

### 构建生产版本
```bash
npm run build     # 构建生产版本到 dist 目录
```

### 预览生产版本
```bash
npm run preview   # 预览构建后的生产版本
```

### 代码检查
```bash
npm run lint      # 运行 ESLint 检查代码质量
```

---

## 六、故障排除

### 问题1：端口被占用
**错误信息**：Port 3000 is already in use

**解决方法**：
1. 关闭占用3000端口的程序
2. 或修改 `vite.config.ts` 中的端口号：
```typescript
server: {
  port: 3001, // 改为其他端口
}
```

### 问题2：依赖安装失败
**解决方法**：
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题3：启动后页面空白
**解决方法**：
1. 检查浏览器控制台是否有错误
2. 确保 Node.js 版本 >= 16
3. 尝试清除浏览器缓存后刷新

### 问题4：热更新不生效
**解决方法**：
```bash
# 重启开发服务器
# Ctrl+C 停止，然后重新运行
npm run dev
```

---

## 七、系统要求

- **Node.js**: >= 16.0.0
- **npm**: >= 7.0.0
- **浏览器**: Chrome/Firefox/Safari/Edge (最新版本)
- **操作系统**: macOS/Windows/Linux

---

## 八、检查环境

运行以下命令检查您的环境：

```bash
# 检查 Node.js 版本
node --version

# 检查 npm 版本
npm --version
```

确保版本符合要求。

---

## 九、开发建议

1. **使用现代浏览器**：推荐使用 Chrome 开发者工具进行调试
2. **保持依赖更新**：定期运行 `npm update` 更新依赖
3. **代码编辑器**：推荐使用 VSCode + ESLint 插件
4. **浏览器插件**：安装 React Developer Tools 方便调试

---

## 十、下一步

✅ 环境搭建完成后，建议：

1. 📖 阅读 `README.md` 了解项目详情
2. 📊 阅读 `数据可视化使用说明.md` 学习如何使用图表功能
3. 🔧 查看 `src/` 目录了解代码结构
4. 🎨 尝试修改代码，体验热更新功能

---

## 📞 需要帮助？

如遇到其他问题，请检查：
- `README.md` - 详细的项目文档
- `数据可视化使用说明.md` - 功能使用指南
- 项目源码中的注释

---

祝您使用愉快！🎉

