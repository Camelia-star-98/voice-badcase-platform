# 🔍 修复优先级功能 - 本地验证指南

## 📋 验证步骤

### 第一步：执行数据库迁移脚本

1. **登录 Supabase Dashboard**
   - 访问：https://supabase.com/dashboard
   - 选择你的项目

2. **打开 SQL Editor**
   - 点击左侧菜单的 **SQL Editor**
   - 点击 **New query**

3. **执行迁移脚本**
   - 打开文件：`database/add_priority_column.sql`
   - 复制全部内容
   - 粘贴到 SQL Editor
   - 点击 **Run** 执行

4. **验证字段添加成功**
   - 执行后应该看到 "✅ Priority 字段添加成功！" 的提示
   - 可以查看表结构确认 `priority` 字段已添加

### 第二步：启动开发服务器

```bash
cd /Users/yanglurui/voice-badcase-platform
npm install  # 如果还没安装依赖
npm run dev
```

访问：http://localhost:5173

### 第三步：功能验证清单

#### ✅ 1. 新建 Badcase 时选择优先级

1. 点击 **"新建Badcase"** 按钮
2. 填写表单信息
3. **验证优先级选择器**：
   - 应该看到 **"修复优先级"** 字段
   - 默认值应该是 **"中"**
   - 可以选择：高、中、低
4. 提交表单
5. **验证**：新建的记录应该包含优先级信息

#### ✅ 2. 列表页面显示优先级

1. 在 Badcase 列表页面
2. **验证优先级列**：
   - 表格中应该有一列 **"优先级"**
   - 显示为带颜色的 Tag（高=红色、中=橙色、低=蓝色）
   - 可以点击列头进行排序（高优先级在前）

#### ✅ 3. 优先级筛选功能

1. 在列表页面的筛选区域
2. **验证优先级筛选器**：
   - 应该有一个 **"全部优先级"** 的下拉选择器
   - 可以选择：全部优先级、高、中、低
3. 选择某个优先级后点击 **"搜索"**
4. **验证**：列表应该只显示对应优先级的记录

#### ✅ 4. 详情 Modal 显示优先级

1. 点击任意记录的 **"查看"** 按钮
2. **验证详情 Modal**：
   - 在状态旁边应该显示 **"优先级"** 字段
   - 显示为带颜色的 Tag

#### ✅ 5. 表格列筛选

1. 在优先级列头点击筛选图标
2. **验证**：
   - 可以选择：高、中、低
   - 筛选后只显示对应优先级的记录

#### ✅ 6. 数据持久化验证

1. 刷新页面
2. **验证**：
   - 之前设置的优先级应该仍然存在
   - 没有设置优先级的旧数据应该显示为 **"中"**（默认值）

## 🐛 常见问题排查

### 问题1：优先级字段不显示

**可能原因**：数据库迁移脚本未执行

**解决方法**：
- 检查 Supabase 表结构，确认 `priority` 字段是否存在
- 如果不存在，重新执行迁移脚本

### 问题2：新建时优先级选择器不显示

**可能原因**：代码未正确加载

**解决方法**：
- 检查浏览器控制台是否有错误
- 确认已重启开发服务器
- 清除浏览器缓存后重试

### 问题3：筛选功能不工作

**可能原因**：筛选逻辑问题

**解决方法**：
- 打开浏览器开发者工具（F12）
- 查看 Console 是否有错误信息
- 检查筛选状态是否正确更新

### 问题4：旧数据没有优先级

**正常现象**：旧数据会使用默认值 "medium"（中）

**如果需要更新旧数据**：
```sql
-- 在 Supabase SQL Editor 中执行
UPDATE public.badcases 
SET priority = 'medium' 
WHERE priority IS NULL;
```

## ✅ 验证完成标准

完成以下所有验证项后，功能即可确认正常：

- [ ] 数据库 `priority` 字段已添加
- [ ] 新建表单可以选择优先级
- [ ] 列表页面显示优先级列
- [ ] 优先级筛选功能正常
- [ ] 详情 Modal 显示优先级
- [ ] 表格列筛选功能正常
- [ ] 数据持久化正常
- [ ] 浏览器控制台无错误

## 🎉 验证通过后

如果所有验证项都通过，可以：

1. **提交代码**：
   ```bash
   git add .
   git commit -m "feat: 添加修复优先级选择和筛选功能"
   git push origin feature/priority-filter
   ```

2. **创建 Pull Request**：
   - 访问 GitHub 仓库
   - 创建从 `feature/priority-filter` 到 `main` 的 PR
   - 填写 PR 描述，说明功能变更

---

**祝你验证顺利！** 🚀





