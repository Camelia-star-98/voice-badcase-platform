# 🚀 完整配置和提交指南

## 📋 第一部分：按照 GitHub 文档配置项目

### 步骤1：配置 Supabase（已完成 ✅）

你已经配置好了：
- ✅ `.env.local` 文件已创建
- ✅ Supabase URL: https://mcpyilgpotajpmgblorc.supabase.co
- ✅ Supabase Key: 已配置

### 步骤2：执行数据库迁移（需要完成）

#### 2.1 创建 badcases 表

1. **访问 Supabase Dashboard**
   ```
   https://supabase.com/dashboard
   ```

2. **选择你的项目**（mcpyilgpotajpmgblorc）

3. **打开 SQL Editor**
   - 点击左侧菜单 **SQL Editor**
   - 点击 **New query**

4. **执行建表脚本**
   - 打开文件：`database/create_badcases_table.sql`
   - 复制**全部内容**
   - 粘贴到 SQL Editor
   - 点击 **Run** 执行

5. **验证表创建成功**
   - 点击 **Table Editor**
   - 确认能看到 `badcases` 表

#### 2.2 添加优先级字段

1. **在 SQL Editor 中执行**
   - 打开文件：`database/add_priority_column.sql`
   - 复制**全部内容**
   - 粘贴到 SQL Editor
   - 点击 **Run** 执行

2. **验证字段添加成功**
   - 在 Table Editor 中查看 `badcases` 表
   - 确认能看到 `priority` 字段

---

## 📋 第二部分：提交修改分支（按照 CONTRIBUTING.md）

### 当前状态

- ✅ 当前分支：`feature/priority-filter`
- ✅ 已修改的文件：多个文件
- ✅ 新增的文件：数据库迁移脚本、文档等

---

### 步骤1：检查当前状态

```bash
cd /Users/yanglurui/voice-badcase-platform
git status
```

**当前状态：**
- 修改的文件：`.env.example`, `.gitignore`, `src/**` 等
- 新增的文件：`database/add_priority_column.sql`, 各种文档等

---

### 步骤2：添加文件到暂存区

#### 方法1：添加所有文件（推荐）

```bash
git add .
```

#### 方法2：选择性添加（更安全）

```bash
# 只添加代码文件
git add src/
git add database/
git add package.json
git add .gitignore

# 添加文档（可选）
git add *.md

# 不要添加 .env.local（已在 .gitignore 中）
```

---

### 步骤3：提交更改

按照 CONTRIBUTING.md 的提交规范：

```bash
git commit -m "feat: 添加修复优先级选择和筛选功能"
```

**提交信息格式：**
- `feat:` - 新功能
- `fix:` - Bug修复
- `docs:` - 文档更新
- `style:` - 代码格式
- `refactor:` - 代码重构

**示例：**
```bash
git commit -m "feat: 添加修复优先级选择和筛选功能

- 添加优先级字段到数据库
- 在新建表单中添加优先级选择器
- 在列表页面添加优先级列和筛选功能
- 支持按优先级排序
- 更新类型定义和API映射"
```

---

### 步骤4：推送到远程仓库

```bash
git push origin feature/priority-filter
```

**如果是第一次推送这个分支：**

```bash
git push -u origin feature/priority-filter
```

`-u` 参数会设置上游分支，以后可以直接用 `git push`

---

### 步骤5：创建 Pull Request

1. **访问 GitHub 仓库**
   ```
   https://github.com/Camelia-star-98/voice-badcase-platform
   ```

2. **创建 Pull Request**
   - 点击 **"Pull requests"** 标签页
   - 点击 **"New Pull Request"** 按钮
   - 选择：
     - **base**: `main`
     - **compare**: `feature/priority-filter`

3. **填写 PR 描述**

   ```markdown
   ## 功能描述
   添加"修复优先级"功能，支持在新建 Badcase 时选择优先级，并在列表页面支持按优先级筛选和排序。

   ## 变更内容
   - ✅ 添加数据库迁移脚本（添加 priority 字段）
   - ✅ 更新 TypeScript 类型定义
   - ✅ 更新 API 映射函数
   - ✅ 在新建表单中添加优先级选择器
   - ✅ 在列表页面添加优先级列（带颜色 Tag）
   - ✅ 添加优先级筛选功能
   - ✅ 支持按优先级排序（高优先级在前）
   - ✅ 在详情 Modal 中显示优先级

   ## 测试
   - [x] 本地测试通过
   - [x] 数据库迁移脚本已执行
   - [x] 新建 Badcase 时可以选择优先级
   - [x] 列表页面显示优先级列
   - [x] 优先级筛选功能正常
   - [x] 优先级排序功能正常

   ## 数据库变更
   - 添加 `priority` 字段（TEXT，默认值：'medium'）
   - 创建 `idx_badcases_priority` 索引
   ```

4. **提交 PR**
   - 点击 **"Create Pull Request"**

---

## 📋 完整命令流程（复制粘贴）

```bash
# 1. 进入项目目录
cd /Users/yanglurui/voice-badcase-platform

# 2. 检查当前状态
git status

# 3. 添加所有文件到暂存区
git add .

# 4. 提交更改（按照规范）
git commit -m "feat: 添加修复优先级选择和筛选功能

- 添加优先级字段到数据库
- 在新建表单中添加优先级选择器
- 在列表页面添加优先级列和筛选功能
- 支持按优先级排序
- 更新类型定义和API映射"

# 5. 推送到远程仓库
git push origin feature/priority-filter

# 如果第一次推送，使用：
# git push -u origin feature/priority-filter
```

---

## ✅ 检查清单

提交前确认：

- [ ] ✅ 代码在本地运行正常
- [ ] ✅ 已测试新增/修改的功能
- [ ] ✅ Commit message 符合规范（feat/fix/docs 等）
- [ ] ✅ 没有提交敏感信息（`.env.local` 已在 .gitignore 中）
- [ ] ✅ 没有提交不必要的文件（`node_modules` 等）
- [ ] ✅ 数据库迁移脚本已创建

---

## 🎯 提交后的步骤

1. **等待代码审查**
   - 团队成员会审查你的代码
   - 根据反馈修改代码

2. **根据反馈修改**（如果需要）
   ```bash
   # 修改代码后
   git add .
   git commit -m "fix: 根据审查反馈修改代码"
   git push origin feature/priority-filter
   ```

3. **PR 合并后**
   - 切换到 main 分支
   - 拉取最新代码
   ```bash
   git checkout main
   git pull origin main
   ```

---

## 🆘 常见问题

### 问题1：推送时提示权限被拒绝

**解决方法：**
- 确认你已被添加为仓库协作者
- 联系项目负责人 @Camelia-star-98

### 问题2：推送时提示冲突

**解决方法：**
```bash
# 1. 拉取最新代码
git pull origin main

# 2. 解决冲突后
git add .
git commit -m "chore: 解决合并冲突"
git push origin feature/priority-filter
```

### 问题3：想撤销提交

**解决方法：**
```bash
# 撤销最后一次提交（保留更改）
git reset --soft HEAD~1

# 撤销最后一次提交（丢弃更改）⚠️ 慎用
git reset --hard HEAD~1
```

---

## 🎉 完成！

完成以上步骤后，你的代码就会提交到 GitHub，并可以创建 Pull Request 了！

---

## 📚 参考文档

- [CONTRIBUTING.md](./CONTRIBUTING.md) - 团队协作指南
- [SUPABASE_SETUP.md](./SUPABASE_SETUP.md) - Supabase 配置指南

